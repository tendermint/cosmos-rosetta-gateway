name: Wasmd Demo Docker
on: push


jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: docker build
        run: |
          cd cosmwasm
          docker build .

   
   
   
   

